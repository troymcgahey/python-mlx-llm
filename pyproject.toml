[project]
name = "python-mlx-llm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "troymcgahey", email = "tmcgahey93@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "mlx>=0.32.2",
]

[project.scripts]
python-mlx-llm = "python_mlx_llm:main"

[build-system]
requires = ["uv_build>=0.11.3,<0.12.0"]
build-backend = "uv_build"
